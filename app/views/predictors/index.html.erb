<h1>Predictors#index</h1>

<p>
  <%= link_to 'New Predictor', predictors_new_path %>
</p>

<p>
  <%= form_with url: "/predictors", method: :get do |form| %>
  <%= form.label :query, "Search for:" %>
  <%= form.text_field :query %>
  <%= form.submit "Search" %>
  <% end %>
</p>

<% @predictors.each do |p| %>
<p>
  <%= link_to p.title, predictor_path(p) %><br>
</p>
<% end %>

