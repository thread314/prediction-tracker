  <strong>Reason:</strong> <%= report.reason %>
  <br>
  <strong>Body:</strong> <%= report.body %>
  <br>
  <strong>Status:</strong> <%= report.status %>
  <br>
  <em>Created by <%= User.find(report.user_id).email %> on <%= report.created_at.to_date %></em>

