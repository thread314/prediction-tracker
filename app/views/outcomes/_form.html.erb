<%= form_with(model: outcome) do |form| %>
  <% if outcome.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(outcome.errors.count, "error") %> prohibited this outcome from being saved:</h2>

      <ul>
        <% outcome.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.select :result, Outcome.results.keys.map { |result| [result.titleize, result] }, { prompt: 'Select a Result' } %>

  </div>

  <div>
    <%= form.label :body, style: "display: block" %>
    <%= form.text_area :body %>
  </div>
  <%= form.hidden_field :prediction_id, value: params[:prediction_id] %>
  <div>
    <%= form.submit %>
  </div>
  <% end %>

