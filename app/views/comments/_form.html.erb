<% if comment.errors.any? %>
<div class: "content has-text-danger">
  <div class: "title is-5"><%= pluralize(comment.errors.count, "error") %> prohibited this comment from being saved:</div>

  <ul>
    <% comment.errors.each do |error| %>
    <li><%= error.full_message %></li>
    <% end %>
  </ul>
</div>
<% end %>

<%= form_with(model: [@commentable, @comment], class: "form") do |form| %>
<%= form.label :body, style: "display: block", class: "label" %>
<div class="field">
  <%= form.text_area :body, class: "textarea" %>
</div>
<%= form.submit class: "button is-link" %>
<% end %>
