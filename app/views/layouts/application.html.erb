<!DOCTYPE html>
<html>
  <head>
    <title>PredictionTracker</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= link_to 'Home', root_path %> 
    <%= if user_signed_in? then current_user.email else link_to("Sign in", new_user_session_url) end %>
    <%= if user_signed_in? then link_to "Sign Out", destroy_user_session_path, data: { "turbo-method": :delete } end %>

    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= yield %>
    <% if Rails.env.development? then console end %>

  </body>
</html>
